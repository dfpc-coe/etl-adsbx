{
    "version": "1.0",
    "name": "ADS-B Exchange",
    "description": "Ingests real-time ADS-B aircraft position data from ADS-B Exchange and posts aircraft tracks to the map.",
    "compute": {
        "memory": 1024,
        "timeout": 120
    },
    "permissions": [
        {
            "resource": "feature:*",
            "required": true,
            "description": "ADS-B Exchange posts aircraft tracks to the map"
        }
    ],
    "invocations": {
        "incoming": {
            "schedule": {
                "description": "Polls ADS-B Exchange for updated aircraft positions on a schedule.",
                "default": {
                    "enabled": true,
                    "schedule": "rate(1 minute)"
                }
            }
        }
    }
}
